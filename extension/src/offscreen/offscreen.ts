// PrivAgent offscreen document (M0 scaffold).
// Reserved for DOM/WASM workloads the service worker cannot run — e.g. local ONNX
// inference (WebGPU→WASM fallback) and DOM parsing. No logic implemented in M0.

export {};
